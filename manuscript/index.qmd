---
title: "Graph Neural Guidance for Capacitated Facility Location: A Reproducible Experimental Protocol"
author:
  - name: Victor Rafael Rezende Celestino
    orcid: 0000-0001-5913-2997
    email: vrcelestino@unb.br
    institute: 1
    affiliations:
      - id: unb
        name: University of Brasilia
  - name: Víctor Alejandro Vargas-Pérez
    orcid: 0000-0001-6803-3608
    email: victorvp@go.ugr.es
    institute: 2
    affiliations:
      - id: ugr
        name: Data Science and Computer Intelligence Institute, University of Granada
  - name: Óscar Cordón García
    orcid: 0000-0001-5112-5629
    email: ocordon@decsai.ugr.es
    institute: 2
    affiliations:
      - ref: ugr
  - name: Pedro González García
    orcid: 0000-0002-6733-3868
    email: pglez@ujaen.es
    institute: 3
    affiliations:
      - id: uja
        name: Data Science and Computer Intelligence Institute, University of Jaén
sbc-affiliations:
  - text: University of Brasilia
    email: vrcelestino@unb.br
  - text: "Data Science and Computer Intelligence Institute, University of Granada"
    email: "victorvp@go.ugr.es, ocordon@decsai.ugr.es"
  - text: "Data Science and Computer Intelligence Institute, University of Jaén"
    email: pglez@ujaen.es
abstract: |
  Learning-assisted mixed-integer optimization requires a clear distinction between the mathematical instance, its solution labels, and the intervention applied to a solver. This manuscript develops an experimental protocol for capacitated facility location using variable-constraint bipartite graphs and a Gasse-inspired graph neural network. Gurobi provides the authoritative model interpretation and root-relaxation features; Gurobi and SCIP supply separately identified solution labels. A symmetric local-branching operator generates training-only synthetic instances from feasible incumbents. The design contrasts original-only and augmented training within each solver-labelled population, while retaining parent-level partitions and independent validation of derived labels. Source diagnostics admitted 39 of 42 development parents (30 easy and nine medium), while three medium cases remained above a 10 percent gap ceiling after 14,400-second repair solves. An explicitly approved revision uses 23 training, eight validation and eight test parents, with 100 epochs and seed 42. Training remains in execution. Nonbinding Gurobi variable hints are the primary guidance mechanism, and terminal MIP gap and optimization wall time are the primary outcomes. This development-stage draft presents the formulation, implementation contracts, source-admission results and remaining analysis plan. Final neural confirmation remains pending; no empirical improvement, complete 90-instance validation, or achieved technology-readiness level is claimed.
---

# Introduction

Mixed-integer linear programming (MILP) combines expressive mathematical models with general-purpose optimization algorithms. Repeatedly solving related instances creates an opportunity to learn useful regularities without replacing the solver's feasibility checks or optimality certificates. Graph neural networks (GNNs) are a natural candidate because variable-constraint incidence exposes the structure of a MILP independently of a fixed matrix size [@cappart2021]. Whether this representation yields useful solver guidance, however, is an empirical question rather than a consequence of accurate variable classification.

We study this question for capacitated facility location (CFL) instances obtained from [MILPBench](https://github.com/thuiar/MILPBench). The research artifact separates five operations: original-instance solution and trajectory collection, mathematical augmentation, graph construction, supervised learning, and held-out solver guidance. This separation is intended to prevent incumbent vectors from being mistaken for new optimization instances and to prevent solver-specific label provenance from changing graph semantics.

The proposed contribution is a reproducible comparison protocol, not a new claim of state-of-the-art solver performance. The research questions are: (i) does incumbent-centred augmentation improve held-out assignment prediction relative to original-only training; (ii) how sensitive is the comparison to the solver supplying the labels and their certified quality; and (iii) do the resulting predictions improve full-model optimization under a fixed budget? This draft specifies how those questions will be answered. Empirical answers remain pending.

# Related work and methodological position

## Learning to Optimize and the role of the solver

Learning to Optimize (L2O) uses experience across optimization problems to learn parts of a solution method. @chen2022primer organize this perspective around learned algorithms, architectures, and training procedures, with particular attention to continuous optimization and distribution-dependent generalization. A method that exploits a recurring problem family need not transfer to arbitrary instances. In the present study, the easy/medium confirmation cohort therefore cannot support a claim about unseen hard CFL instances or other MILP families.

@chen2024tutorial distinguish accelerating optimization algorithms, estimating solutions, and adapting optimization models to application objectives. Their treatment of mixed-integer optimization motivates combining a predictor with conventional solver components rather than treating a raw network output as a feasible, certified solution. Following the research project's L2O motivation, our focus is algorithm acceleration through solution-informed guidance: offline supervision predicts binary assignments, while the online solver interprets hints or candidate starts, checks feasibility, and maintains bounds. We do not learn the economic CFL objective, replace branch-and-bound, or differentiate through an optimization layer. Nor is the Adam optimizer used to train the GNN itself a learned optimizer.

The distinction matters beyond linear models. @tang2024l2o study direct solution generation for parametric mixed-integer nonlinear programs, with integer-correction layers and a feasibility-restoration mechanism. That work supplies a contrasting L2O design, not evidence that unconstrained assignment predictions in our CFL pipeline satisfy MILP constraints. Its problem setting and guarantees are not transferred to our model. Here, a feasible point and a solver-derived bound are separate audited artifacts; a low prediction loss alone certifies neither feasibility nor optimality.

This positioning also narrows the broader research agenda. Learned configuration, cut selection, attention-based architectures, and explanation methods are possible extensions, not implemented contributions of this manuscript. Training and validation curves test the predictor; held-out gap and time measurements test its usefulness inside optimization. Graph projections remain descriptive and are not, by themselves, explanations of solver behaviour.

## Graph learning for distinct MILP decisions

@gasse2019 introduce a variable-constraint bipartite graph convolutional architecture for learning branching decisions through imitation of strong branching. We retain that paper as the principal architectural reference. Our supervised targets are binary solution values, not expert branching actions; consequently, neither the training task nor the downstream intervention is a replication of their experiments.

Other graph-learning tasks operate on different decisions and information. @labassi2022 learn a pairwise branch-and-bound node comparator using a siamese GNN; this ranks search nodes rather than predicting the values of original-model variables. @scavuzzo2024 learn optimal objective values using dynamic information. Such predictions address a different target from binary supervision and do not replace the solver's primal-dual certificate in our protocol. These distinctions prevent the shared use of a bipartite graph from being mistaken for an identical learning problem.

Representation capacity also requires qualification. @chen2024branching identify limitations of message-passing GNNs for representing strong-branching scores and study a higher-order alternative. @chen2024quadratic analyse graph expressivity for continuous and mixed-integer quadratic programs, distinguishing their representable classes. Neither result establishes that our Gasse-inspired network can represent every optimal CFL assignment. We use these studies to motivate explicit architecture, symmetry, and distributional limitations, not to claim a new universality theorem or to add an untested quadratic-programming capability.

## Primal prediction and neighbourhood guidance

Solution prediction has also been used to guide primal search. @ding2020 combine predicted binary values with a local-branching-type intervention. @khalil2022 predict variable biases from near-optimal solutions and use them to guide solver components. The present protocol uses a named, independently checked solution as the label for each mathematical instance; it does not identify that target with the solution-distribution biases of MIP-GNN. @canturk2024 provide a further closely related study of GNN-based primal heuristics. These works motivate separating prediction quality from the quality of solutions ultimately produced by a solver.

Neural Diving generates partial assignments and delegates the remaining optimization to a solver [@nair2020]. It must not be conflated with nonbinding variable hints. The primary intervention here leaves the original feasible region unchanged. Restrictive interventions are exploratory and require subsequent full-model recovery. Local branching [@fischetti2003] supplies a mathematical neighbourhood operator, used here both as a source of synthetic training instances and, separately, as a possible search intervention. These two uses have different provenance and evaluation requirements.

# Mathematical instances, labels, and augmentation

## Authoritative formulation and objective convention

For parent instance $p$, write the file-defined MILP as

$$
\begin{aligned}
z_p^\star &= \min_x \; c_p^\top x+c_{p,0},\\
A_p x &\;\mathrel{\bowtie_p}\; b_p,\\
\ell_p \leq x &\leq u_p,\qquad x_j\in\mathbb{Z}\quad(j\in I_p).
\end{aligned}
$$

Here $\bowtie_p$ records each row's original sense, $I_p$ denotes integral variables, and $B_p\subseteq I_p$ denotes canonical binary variables. The benchmark file, not a newly written textbook CFL formulation, defines the coefficients, objective constant, variable domains, and constraint identities. This avoids asserting an unverified correspondence between benchmark columns and a simplified facility-assignment model.

The study applies an explicit MINIMIZE override to every CFL input, following the project's correction of the original MAXIMIZE headers. The original file is preserved, and both objective senses and its hash are recorded. This is a disclosed study convention, not an independently established assertion about every future MILPBench release. All labels, root features, and evaluation runs must refer to the corrected formulation.

## Independent label admission

A solution label is admitted only after checking variable-name coverage, finite values, bounds, integrality, every linear row, and the reconstructed objective including its constant. Its parent-model identity must match the source receipt. A finite terminal solver-relative gap at most $0.10$ is necessary but not sufficient. Sensitivity analyses retain thresholds $0.01$, $0.05$, $0.06$, and $0.10$ without retrospectively discarding difficult parents to improve the apparent outcome.

Terminal gap, gap at incumbent discovery, total solve time, and incumbent discovery time have distinct meanings. A later dual-bound improvement can reduce the terminal gap without producing a new incumbent. Historical Unix timestamps cannot be reported as elapsed optimization time. Native gap definitions and a separately specified common comparison gap are retained; they must not be silently interchanged across solvers. A timeout with a feasible, admissible label is different from a failed computation or an optimality certificate.

## Synthetic instances from feasible incumbents

Given a feasible binary centre $\bar{x}$ and integer radius $k$, the local-branching constraint is

$$
\Delta_p(x,\bar{x})=
\sum_{j\in B_p:\bar{x}_j=0}x_j+
\sum_{j\in B_p:\bar{x}_j=1}(1-x_j)\leq k.
$$

The synthetic feasible set is $X_{p,k}=X_p\cap\{x:\Delta_p(x,\bar{x})\leq k\}$. The selected fractions are $0.001$, $0.005$, and $0.01$ of the binary support; the materialized integer radius and constraint hash are recorded. At most three distinct radii are retained per parent and solver. The same operator is applied to Gurobi and SCIP centres, although the centres and resulting inequalities need not coincide.

Each derived MILP receives an independent solve without supplying its centre as a warm start. Its label must satisfy the derived formulation and the local-branching inequality. The centre is used to define the neighbourhood, not to fabricate the derived label. Graphs are constructed for the parents and these explicitly modified models, never for every incumbent in a trajectory. A distinct formulation need not have a distinct optimum or label; augmentation diversity therefore requires its own audit. Derived instances are correlated descendants, not independent experimental replicates.

# Graph construction and supervised learning

## Gurobi-authoritative bipartite representation

Each graph contains variable and constraint nodes, with one coefficient-bearing edge per nonzero matrix entry. The variable features encode the objective coefficient, lower and upper bounds, variable type, and a real Gurobi root-node LP-relaxation value. Constraint features encode the right-hand side and row sense; edge features encode coefficients. The implementation records feature order, scaling, clipping, and raw-domain checks, rather than claiming the encoded representation is universally lossless.

Gurobi is the graph authority for both label sources. In particular, a SCIP-labelled graph does not use a separate SCIP feature extractor. The root vector is captured at a valid root MIPNODE observation and aligned by variable identity. It is a solver-state observation, not an incumbent label, and is not replaced by zeros when capture fails. Root preprocessing time is an additional computational cost that must be reported before making an end-to-end acceleration claim.

Graph statistics include parent counts, variable and constraint counts, nonzeros, degree summaries, domain features, and binary-target prevalence. Structural projections and clustering use outcome-free graph descriptors. They are descriptive summaries, not learned GNN embeddings, and must not select folds, labels, or guidance settings using the test set. Degenerate rank or clustering is reported explicitly rather than rendered as apparent class separation.

## Versioned Gasse-inspired predictor

The legacy Gasse implementation is preserved by source identity. Confirmation uses the separately versioned alternating-prenorm correction: variable-side normalization is fitted using updated constraint messages. Streaming normalization statistics use training graphs only. Numerical verification includes the original toy bipartite fixture, finite gradients, overfitting on a toy example, checkpoint replay, and permutation/disjoint-union behaviour. Passing these checks does not establish predictive usefulness on the CFL cohort.

For binary label $y_j$ and logit $s_j$, training uses positive-class-weighted binary cross-entropy,

$$
\ell_j=-w_+y_j\log\sigma(s_j)-(1-y_j)\log(1-\sigma(s_j)).
$$

The positive weight is estimated from training targets. General integer or fractional values are not coerced into binary labels. Weighted scores are not assumed to be calibrated probabilities. The confirmation configuration specifies two message-passing layers, hidden dimension 32, Adam with learning rate $10^{-3}$, gradient clipping at norm 1, seed 42, and 100 full epochs. The checkpoint minimizes validation weighted BCE; a decision threshold maximizes validation F1. Neither choice uses held-out test labels.

Original-only and augmented arms receive equal parent-level sampling mass and equal optimizer-step budgets. Within an augmented arm, multiple descendants do not increase their parent's total sampling weight. The broader Gurobi confirmation run and the paired four-arm experiment remain separate analyses with separately reported populations.

# Experimental design and analysis plan

## Populations and leakage controls

The frozen development confirmation cohort comprises 30 easy and 12 medium parents, with no hard instances. Rotation 0 originally assigns 24 parents to training, 10 to validation, and eight to test. The strict 42-parent campaign remains incomplete. Following explicit project-owner approval, a separate 39-parent revision retains 30 easy and nine medium parents, with 23 training, eight validation and eight test parents. Existing roles are preserved; no parent is reassigned and no easy-only substitution is made. This outcome-dependent revision does not turn the original campaign into a successful 42-parent experiment. Original evidence and exclusions remain preserved. The later 90-parent campaign is outside the current execution scope.

For the paired experiment, the four training arms are Gurobi original-only, Gurobi augmented, SCIP original-only, and SCIP augmented. Their common eligible parent intersection is reported explicitly, including exclusions and censoring. All descendants inherit the parent fold and remain training-only. Validation and test contain originals only, with a shared evaluation-reference policy. Solver-specific labels can disagree; this is not automatically a model defect, especially when multiple optima exist. Accordingly, assignment agreement and optimization feasibility/objective quality answer different questions.

## Neural guidance and solver budgets

The primary comparison is Gurobi variable hints against unguided Gurobi on the same held-out original instance. Native hint values and priorities guide search without imposing bounds; partial MIP starts instead provide candidate solution information [@gurobi2026]. SCIP is a matched comparison solver through PySCIPOpt, not an alternative graph authority, and is not assigned a fictitious equivalent of Gurobi's hint attributes.

Precommitted exploratory methods include partial starts, confidence-based partial fixing, and local-branching trust regions. Binding methods allocate 20 percent of the optimization budget to the restricted phase and then execute fresh original-model recovery with the remaining optimization time. Only primal information can be transferred; a restricted-model dual bound is not a certificate for the full model. Coverage fractions are 1, 5, and 10 percent. Solver budgets are 3,600 or 14,400 seconds, with one thread per run and seed 42. Each budget is analysed separately; selecting a budget after observing test performance would change the experiment.

## Outcomes and figures

The primary outcomes are terminal full-model MIP gap and wall time inside optimization. Total wall time, data reading, model construction, and optimization are recorded as distinct regions; unallocated overhead is retained when reconciling the total. Inference and graph-preparation costs must also be disclosed for any end-to-end comparison. Solver-native runtime and externally timed optimization are not interchangeable measurements.

Timeouts are retained as right-censored time-to-completion observations, not counted as completed solves at the time limit. Terminal gap remains an observed outcome at that budget. Pairing is by parent and target solver; descendants and individual binary variables do not inflate the number of independent test units. The current single-seed development study supports descriptive inspection, not a universal superiority claim or a stable estimate of training-seed variability.

The intended outputs are specified in @tbl-output-plan. Final numerical tables and figures require an accepted, hash-bound artifact ledger. Interim source diagnostics below are explicitly transcribed from supplied audit evidence rather than independently revalidated remote artifacts. No example measurements or simulated loss curves are substituted while the campaign is running.

| Stage | Planned output | Interpretation safeguard |
|:--|:--|:--|
| Collection | Instance descriptors, incumbent trajectories, gap and timing summaries | Separate terminal and discovery measurements |
| Graphs | Structural statistics, dimensionality reduction, clustering | Exclude labels and solver outcomes from projection inputs |
| Training | Training and validation loss on the same axes over 100 epochs | Distinguish online training loss from post-epoch validation loss |
| Prediction | Precision, recall, F1, average precision, BCE, Brier score and ECE | Report parent-macro and pooled metrics, prevalence and trivial baselines |
| Optimization | Paired gap and time outcomes, budget sensitivity and failure counts | Retain censoring and distinguish full from restricted models |

: Planned manuscript outputs; no measured results are represented in this table. {#tbl-output-plan}

The prediction analysis includes constant-zero and root-relaxation diagnostics. Accuracy alone is insufficient for sparse positive targets. Calibration diagnostics do not retrospectively turn weighted-BCE outputs into calibrated probabilities. High classification agreement can coexist with infeasible rounded assignments, and a different assignment can attain the same optimal objective.

# Results: source evidence and pending neural confirmation

**Development-stage draft: empirical confirmation pending.** The evidence cut-off is 13 September 2026. The following results distinguish supplied source-admission diagnostics, operational Slurm status, and measurements not yet available. The sanitized extract in `results/source_evidence.json` retains the campaign identity and referenced report hashes. Those hashes trace the supplied diagnostic; their inclusion does not mean that every underlying remote file has been independently downloaded and revalidated for this manuscript. The final acceptance bundle remains outstanding.

## Source admission and cohort revision

The diagnostic reports 39 admitted parents with accepted artifact hashes valid. Admission uses a terminal relative MIP gap of at most 0.10, not the mere presence of a feasible incumbent. All three exclusions are medium instances. The retained fraction is 39/42 (92.9 percent) overall, 30/30 among easy instances and 9/12 among medium instances. These are cohort-specific admission proportions, not estimates of solver success on MILPBench as a whole.

| Partition | Original cohort | Excluded | Revised cohort |
|:--|--:|--:|--:|
| Training | 24 | 1 | 23 |
| Validation | 10 | 2 | 8 |
| Test | 8 | 0 | 8 |
| Total | 42 | 3 | 39 |

: Parent-level cohort revision. Roles were inherited rather than repartitioned; exclusion was based on source-label admissibility. {#tbl-cohort}

![Original and revised parent inventories, by difficulty and role. Counts are transcribed from supplied diagnostic evidence and the approved revision; they are not a trained-model result.](figures/figure_cohort_admission.png){#fig-cohort width=100% fig-pos="H"}

The rejected cases are CFL medium instances 3, 5 and 6. The supplied evidence contains six independent repair runs: one 3,600-second and one 14,400-second attempt per instance. Every attempt ended at the time limit. Their solution and variable-order hash checks are reported as valid, with no missing collector checks or source errors. Their rejection is a label-quality outcome, not evidence of infeasible models or corrupt files. The diagnostic audit itself executed no additional solver runs.

| Medium instance | Role | Gap at 3,600 s (%) | Gap at 14,400 s (%) |
|:--|:--|--:|--:|
| 3 | Training | 17.2685 | 12.8804 |
| 5 | Validation | 15.3026 | 13.5790 |
| 6 | Validation | 18.4423 | 13.7285 |

: Terminal MIP gaps for excluded parents. Both budgets are right-censored for time to optimality; all final gaps remain above 10 percent. {#tbl-rejections}

![Terminal gaps of excluded parents under two repair budgets. Bars represent separate runs, not samples along one continuous trajectory. The dashed line marks the admission ceiling.](figures/figure_source_gap.png){#fig-source-gap width=100% fig-pos="H"}

Longer attempts reduced the observed terminal gap in these three cases but did not establish optimality or satisfy admission. These selected difficult cases are not an unbiased solver-comparison sample. The retained cohort is easier under the acceptance criterion, and conclusions must remain conditional on that selection.

## Execution time and retained unsuccessful attempts

Four-region timing is available for all six repair attempts (@fig-source-times). Optimization dominates their recorded duration. Small limit overruns and additional reading, construction and orchestration costs remain visible in the full-precision CSV. Total time is not replaced by solver-native runtime; residual overhead is not silently assigned to construction.

![Four timing regions for six source-repair attempts. M3, M5 and M6 denote excluded medium parents; 1h and 4h denote requested budgets. Panels have independent vertical scales. These are source-label repair costs, not GNN-guided solver outcomes.](figures/figure_source_time_regions.png){#fig-source-times width=100% fig-pos="H"}

The accompanying [source-rejection outcome table](tables/table_source_rejection_outcomes.csv) retains objective, bound, relative gap, all four time regions, censoring and source-report identity. Keeping unsuccessful attempts prevents the revised cohort from concealing resources spent on excluded cases. End-to-end accounting must also include source collection, graph preparation, training and prediction; those costs cannot be inferred from these six records alone.

## Graph preparation: operational completion and reserved analysis

The supplied Slurm record reports successful completion of all 39 graph tasks and the consolidation job. This establishes scheduler-level completion. Final graph-artifact validation, descriptor tables and visual inspection of the 39-parent figures require the returned evidence bundle. Successful process exit does not certify variable alignment, root-feature provenance, graph uniqueness or projection interpretation.

**Reserved graph-results panel.** Insert variable/constraint counts, nonzeros, degree and domain summaries, binary-target prevalence and within-parent comparisons. Include descriptor projection and clustering views with method, scaling, retained variance where applicable, population, seed and degeneracy disclosed. Distinguish originals from descendants. No projection is rendered here because accepted numerical descriptors have not yet been supplied. Test outcomes must not determine representation, clusters, selection or cohort.

## Neural training and held-out evaluation: pending

At the last supplied observation, job 3307 was RUNNING with elapsed wall time 08:12:14. A running job's zero exit-code field is not successful completion. No completed-epoch count, accepted loss history, selected checkpoint or held-out score is reported here. The configuration remains 100 full epochs, seed 42, 23 training parents and eight validation parents. Eight test parents are reserved for subsequent held-out evaluation.

**Reserved training-results panel.** Insert training and validation weighted BCE on the same axes for every completed epoch; mark the validation-selected checkpoint and intended 100-epoch budget. Retain the actual completed budget if interrupted or unsuccessful. Export the epoch table and distinguish online training loss from post-epoch validation loss. Illustrative curves, repeated endpoints and zero-filled missing epochs are not used.

**Reserved prediction-results panel.** Report per-parent and per-difficulty precision, recall, F1, average precision, BCE, Brier score and calibration error, with parent-macro and pooled summaries where supported by accepted artifacts. Include precision-recall and calibration plots, threshold provenance, prevalence, confusion counts and constant-zero/root-LP diagnostics. ROC summaries, if produced, are secondary to precision-recall under imbalance. Undefined metrics must be labelled. Checkpoint and threshold selection remain validation-only.

## Solver guidance and the complete output inventory

No paired native-guidance effect based on this training run is available. This draft reports neither neural speedup nor a winning augmentation arm. Earlier engineering experiments are not pooled with the revised confirmation. The four-arm protocol and the present Gurobi-labelled confirmation remain distinct analyses.

**Reserved optimization-results panel.** Insert unguided/hinted paired gaps and optimize times, four timing regions, gap sensitivity and paired effects only after matching parent, target solver, budget and policy. Separate 3,600-second from 14,400-second budgets and retain failures and censoring. Restricted bounds are not full-model certificates. Missing comparisons cannot be inferred from classification quality.

@tbl-results-register identifies all output families, including those deferred beyond this partial MVP. Exact filenames and population requirements appear in the manuscript results register. Pending rows are reserved deliverables, not numerical evidence.

| Output family | Tables and figures | Status at cut-off |
|:--|:--|:--|
| Source admission | Cohort table/figure; rejection gap table/figure | Supplied diagnostic |
| Source costs | Repair CSV; four-region figure | Six repair records |
| Full source EDA | Descriptors, incumbent trajectories, gap/time distributions | Bundle pending |
| Graph EDA | Structural statistics, projection, clustering | Artifact review pending |
| Training | Epoch table; shared training/validation loss figure | Job running |
| Prediction | Test/per-parent quality; PR/calibration figures | Evaluation pending |
| Native solver runs | Outcome/time tables; gap/time figures | Current-cohort comparison unavailable |
| Paired effects | Descriptive-effect table and figure | Current-cohort comparison unavailable |
| Gap sensitivity | Admission/solver sensitivity table and figure | Full output pending |

: Publication-output register, separating available diagnostics from unobserved training and solver outcomes. {#tbl-results-register}

# Discussion, limitations and MVP closure

The confirmed advance is source-admission bookkeeping with an explicit 39-parent revision and preserved evidence for three rejected parents. Operational graph completion is also reported. These support an inspectable development workflow, not a conclusion that the GNN predicts useful assignments or accelerates optimization. Completed training would still require loss-history checks, validation-based selection and untouched held-out evaluation.

Threats to validity include solver-conditioned selection, correlated descendants, class imbalance, multiple equivalent optima, outcome-dependent missingness, root-feature acquisition overhead, one training seed and no hard instances. Excluding one training and two validation parents changes both learning and model-selection populations even though test identities remain fixed. A common feature extractor does not make solver-labelled populations statistically equivalent. The MINIMIZE correction also limits external comparisons unless formulation conventions are checked.

The agreed delivery is a research MVP with explicit limitations. It may close with documented partial or unsuccessful experiments, but failed checks remain failed and unavailable results remain unavailable. Software delivery is distinct from experimental success and scientific certification. Native-guidance comparisons and the deferred 90-parent extension are future evidence gates, not results to invent to close this draft. Code review, unit tests and document rendering do not establish scientific validity or TRL 6.

# Reproducibility and availability

The implementation is maintained in the public [CFL-Gurobi-GNN repository](https://github.com/unb-lamfo-or-ai-hpc/cfl-gurobi-gnn). The root README preserves the MILPBench instance-download links. Raw models, solver licences, host paths, checkpoints, and full solution trajectories are not bundled into this manuscript website. Public access to source code does not imply redistribution rights for benchmark data.

The project's original code, documentation and manuscript are released under the **MIT License**. Original project-generated metadata, tables and figures use MIT within the authors' rights. This is not a GNU GPL designation. MILPBench data, solver software and third-party materials retain their own terms; the MIT notice does not relicense source instances or guarantee redistribution rights for their derivatives. Licensing notices accompany reviewed exports without changing immutable experimental receipts.

The manuscript reuses the supplied [quarto-sbc template](https://github.com/cvictorr2508/quarto-sbc), version 0.2.0, pinned by commit. Its original licence and SBC third-party notices accompany the source. A static Quarto Manuscript build produces HTML and PDF without executing the research pipeline. Audited empirical assets can be added later with source hashes and captions that identify their cohort and development status. Author order, affiliations, ORCID identifiers, and email addresses were supplied by the corresponding project contributor. Funding, contribution statements, and the intended journal remain to be confirmed before submission. The current SBC presentation is a working format, not an Elsevier submission template.

# Conclusion

This protocol separates solver-labelled supervision, mathematical augmentation, graph representation, and solver intervention in a CFL research pipeline. Its central safeguards are parent-level partitioning, independent label validation, a common Gurobi graph authority, versioned neural computation, and paired outcomes with explicit censoring. The present contribution is an inspectable experimental specification, source-admission evidence for a revised 39-parent development cohort, and a publication artifact. Whether augmentation or neural hints improve optimization remains to be established by the pending campaign and, subsequently, by the deferred full-population study.

# Declaration of generative AI and AI-assisted technologies in the manuscript preparation process {.unnumbered}

During the preparation of this work, the authors used Gurobot, Gemini 3.1 Pro, and ChatGPT Codex with models 5.6 Sol and 6.0 Astra, to assist with the codebase implementation and to support the preparation, structuring, language refinement, and data visualization of this manuscript. After using these tools/services, the authors reviewed and edited the content as needed and took full responsibility for the content of the published article.

# References
